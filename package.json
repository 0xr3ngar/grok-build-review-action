{
    "name": "grok-build-review-action",
    "private": true,
    "type": "module",
    "scripts": {
        "typecheck": "bunx tsc --noEmit",
        "test": "bun test",
        "fmt": "oxfmt",
        "fmt:check": "oxfmt --check"
    },
    "dependencies": {
        "zod": "^4.4.3"
    },
    "devDependencies": {
        "@types/bun": "^1.3.0",
        "oxfmt": "^0.54.0",
        "typescript": "^6.0.3"
    }
}
